# copier_demo
a dummy copier template repo to showcase functionality


### steps to showcase
```bash
uv tool install copier
copier copy ~/path/to/copier-template ~/path/to/my-new-project --trust # The --trust flag is required because the template runs tasks (shell commands) during generation.

# answer 
```
